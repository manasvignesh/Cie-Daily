# Optional client diagnostics configuration. Do not put server secrets here.
SENTRY_DSN=

# Optional public Edge Function base URL override (not a credential).
# Production already defaults to the deployed CIE Daily backend.
# --dart-define=CIE_TRUSTED_BACKEND_URL=https://PROJECT_REF.supabase.co/functions/v1
